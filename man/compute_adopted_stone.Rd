% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_adopted_stone.R
\name{compute_adopted_stone}
\alias{compute_adopted_stone}
\title{Compute Adopted Stone Dimensions and Quantities}
\usage{
compute_adopted_stone(channel_dims, stone_metrics, method = "nrcs")
}
\arguments{
\item{channel_dims}{data.frame; Wide-format channel dimensions produced by
\code{compute_channel_dimensions()}. Must include columns \code{id}, \code{stone_density},
\code{gravity}, \code{contingency}, \code{porosity}, \code{length_left_bank}, \code{width},
\code{length}.}

\item{stone_metrics}{data.frame; Long-format stone metrics produced by
\code{compute_stone_metrics()}. Must include columns \code{id}, \code{method}, and
\code{stone_diameter}.}

\item{method}{character; The sizing method whose stone diameter is
adopted. One of \code{"nrcs"}, \code{"usace"}, \code{"abt_johnson"}, \code{"isbash"},
\code{"usbr"}. Default: \code{"nrcs"}.}
}
\value{
A \code{tibble} (wide format) with one row per scenario point, containing
all columns from \code{channel_dims} plus the following adopted-stone columns:
\code{adopted_stone_diameter}, \code{adopted_stone_weight_kg},
\code{adopted_stone_weight_lbs}, \code{adopted_stone_weight_ton},
\code{mattress_thickness}, \code{stone_vol_m3}, \code{stone_vol_cuyd},
\code{stone_vol_metric_ton}, \code{stone_vol_us_ton}, \code{number_stones}.
}
\description{
Using the stone diameter from a chosen sizing method, compute
the adopted stone dimensions, weights, mattress thickness, and stone
volume quantities for a chute design.
}
\details{
The adopted stone diameter is taken directly from the stone diameter
computed by the chosen empirical method in \code{compute_stone_metrics()}.
No additional multiplier is applied; design contingency on volume is
applied via the data-driven \code{contingency} column inherited from the
scenario.

\strong{Adopted stone weight equation:}
\deqn{
  W_{kg} = \frac{d^3 \cdot \pi \cdot \gamma_s}{6 \cdot g}
}
where:
\itemize{
\item \eqn{d}: Adopted stone diameter (m)
\item \eqn{\gamma_s}: Stone specific weight = \eqn{stone\_density \times gravity} (N/m^3)
\item \eqn{g}: Gravitational acceleration (m/s^2)
}

\strong{Stone volume equation:}
\deqn{
  V_{m^3} = contingency \times mattress\_thickness \times
            (length\_left\_bank + width) \times length \times (1 - porosity)
}
}
