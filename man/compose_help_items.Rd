% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose_help_items.R
\name{compose_help_items}
\alias{compose_help_items}
\title{Compose one or more help items into a Shiny UI fragment}
\usage{
compose_help_items(
  ids = NULL,
  ...,
  data = NULL,
  data_name = NULL,
  package = NULL,
  fields = c("summary", "detail"),
  include_title = FALSE,
  title_field = "title",
  title_tag = shiny::tags$strong,
  separator = shiny::tags$hr(),
  container = shiny::tagList,
  on_missing = c("error", "warn", "drop"),
  deduplicate = FALSE
)
}
\arguments{
\item{ids}{character vector of help record ids to compose. May also be
supplied through \code{...}.}

\item{...}{additional help record ids supplied as character scalars.}

\item{data}{optional data.frame or tibble containing help records.}

\item{data_name}{optional character scalar giving the name of a dataset
exported by an R package.}

\item{package}{optional character scalar giving the package name containing
\code{data_name}.}

\item{fields}{character vector of fields to render from each help item.
Default: \code{c("summary", "detail")}.}

\item{include_title}{logical; if \code{TRUE}, prepend each help item with its
title. Default: \code{FALSE}.}

\item{title_field}{character scalar naming the title field. Default: \code{"title"}.}

\item{title_tag}{function used to wrap titles. Default: \code{shiny::tags$strong}.}

\item{separator}{optional UI tag inserted between help items. Default:
\code{shiny::tags$hr()}. Use \code{NULL} for no separator.}

\item{container}{function used to combine all UI parts. Default:
\code{shiny::tagList}.}

\item{on_missing}{how to handle ids not present in the help data. One of
\code{"error"}, \code{"warn"}, or \code{"drop"}. Default: \code{"error"}.}

\item{deduplicate}{logical; if \code{TRUE}, remove duplicate ids while preserving
order. Default: \code{FALSE}.}
}
\value{
A Shiny UI object.
}
\description{
Compose one or more help items into a Shiny UI fragment
}
\details{
This function is the composition layer for parameterized and composable help.
It is intentionally independent of specific UI placement concepts such as
popovers or sidebars; those concerns belong in the UI layer.

Help data must contain at least an \code{id} column and any requested \code{fields}.
}
